{
  "name": "dagskra",
  "private": true,
  "scripts": {
    "build": "spago build",
    "release": "spago build && esbuild static/index.js --bundle --outfile=static/bundle.js",
    "repl": "spago repl",
    "start": "spago build && esbuild static/index.js --bundle --outfile=static/bundle.js",
    "test": "spago test"
  },
  "devDependencies": {
    "esbuild": "^0.14.39",
    "parcel": "^2.5.0",
    "purescript": "^0.15.2",
    "purs-tidy": "^0.9.0",
    "spago": "^0.20.9"
  }
}
