{
  "name": "dagskra",
  "private": true,
  "scripts": {
    "build": "spago build && vite build",
    "compile": "spago build",
    "preview": "spago build && vite preview",
    "repl": "spago repl",
    "start": "spago build && vite --open",
    "test": "spago test"
  },
  "devDependencies": {
    "bootstrap": "^5.3.1",
    "bootswatch": "^5.3.1",
    "vite": "^4.4.9"
  }
}
