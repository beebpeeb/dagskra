{
  "name": "dagskra",
  "private": true,
  "source": "assets/index.html",
  "scripts": {
    "build": "spago build",
    "release": "spago build && parcel build",
    "repl": "spago repl",
    "start": "spago build && parcel --open",
    "test": "spago test"
  },
  "devDependencies": {
    "parcel": "^2.7.0"
  }
}
