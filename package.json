{
  "name": "dagskra",
  "private": true,
  "source": "assets/index.html",
  "scripts": {
    "build": "spago build",
    "release": "spago build && parcel build",
    "repl": "spago repl",
    "start": "spago build && parcel --open",
    "test": "spago test"
  },
  "devDependencies": {
    "parcel": "^2.6.1",
    "purescript": "^0.15.2",
    "purs-tidy": "^0.9.0",
    "spago": "^0.20.9"
  }
}
