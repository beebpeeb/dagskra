{
  "name": "dagskra",
  "private": true,
  "scripts": {
    "build": "spago build && vite build",
    "compile": "spago build",
    "preview": "spago build && vite preview",
    "repl": "spago repl",
    "start": "spago build && vite",
    "test": "spago test"
  },
  "devDependencies": {
    "bootstrap": "^5.2.3",
    "bootswatch": "^5.2.3",
    "purescript": "^0.15.7",
    "purs-tidy": "^0.9.2",
    "spago": "^0.20.9",
    "vite": "^4.1.1"
  }
}
